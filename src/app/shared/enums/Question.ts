export enum QuestionType {
    InputText,   // Use camel case for enum names
    MultiChoice  // Consistent naming for enums
  }

  export enum QuestionFor {
    AdditionalInfo, // Correct spelling: "Additional"
    Quiz
  }